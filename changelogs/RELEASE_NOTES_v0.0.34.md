## v0.0.34 — 蜗牛会验货了 🔍

蜗牛新学了一门手艺：看得懂 ILCD、JSON-LD、TIDAS 格式的 LCA 数据包。

丢给它一个数据包——它能告诉你里面有什么数据集、格式对不对、哪里有问题。还能在三种格式之间互转。

这个技能会在你第一次登录时自动安装，不用手动操作。

### 🔍 LCA Format Validator（自动安装）
- **解析**：打开 ILCD/JSON-LD/TIDAS 数据包，摘要展示所有 process 和 flow
- **校验**：UUID、必填字段、交叉引用、分类层级、中英文语言约束，生成 Markdown + JSON 报告
- **互转**：ILCD ↔ JSON-LD 双向转换，覆盖全部 7 种数据集类型

### 🤖 Core 技能自动安装
- 标记为 Core 的市场技能会在登录时自动下载安装
- 不可关闭、不可卸载，云端更新后刷新即可升级

蜗牛会验货了。虽然验的是 XML。🐌

### 平台
- **macOS** — arm64 | **Windows** — x64 | **Linux** — x64

---

The snail learned quality inspection: it can read ILCD, JSON-LD, and TIDAS data packages.

Drop a data package — it tells you what's inside, checks the format, flags issues. Converts between all three formats too.

Auto-installed on first login. No manual setup.

### 🔍 LCA Format Validator (auto-installed)
- **Parse**: summarize all processes and flows in ILCD/JSON-LD/TIDAS packages
- **Validate**: UUID format, required fields, cross-references, classification, language constraints → Markdown + JSON report
- **Convert**: ILCD ↔ JSON-LD bidirectional, all 7 dataset types

### 🤖 Auto-install Core Skills
- Marketplace skills marked as Core auto-download on login
- Cannot be disabled or uninstalled; refresh to get updates

The snail does QC now. On XML files. 🐌

### Platforms
- **macOS** — arm64 | **Windows** — x64 | **Linux** — x64
