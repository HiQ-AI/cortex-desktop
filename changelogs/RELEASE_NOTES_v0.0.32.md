## v0.0.32 — 蜗牛不限量了 ♾️

126 条材料的 BOM 匹配，跑到一半又断了。预算上限从 $0.5 提到 $5 还是不够。

干脆取消了。蜗牛现在没有单次预算限制，多大的活都能跑完。

蜗牛不限量了。钱包在哭。🐌

### 平台
- **macOS** — arm64 | **Windows** — x64 | **Linux** — x64

---

126-material BOM matching kept hitting the budget cap. $0.5 wasn't enough. $5 wasn't enough either.

Removed it entirely. No per-query spending limit. The snail runs until the job is done.

Unlimited snail. Wallet is crying. 🐌

### Platforms
- **macOS** — arm64 | **Windows** — x64 | **Linux** — x64
